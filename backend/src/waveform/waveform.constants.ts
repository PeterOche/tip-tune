export const WAVEFORM_QUEUE = 'waveform-generation';

export const WAVEFORM_JOBS = {
  GENERATE: 'generate-peaks',
};

export const WAVEFORM_JOB_DEFAULTS = {
  ATTEMPTS: 3,
  BACKOFF_DELAY_MS: 5000,
};
